/**
 * Module without custom path resolution in submodule config, and therefore 
 * name of generated module should simply reflect the path of each file 
 * - path moduleC/feature.js
 * 
 * @submodule
 */

/**
 * Module feature function with private access and inner scope
 * 
 * @function
 * @private
 * @inner
 */
const moduleFeature = () => {}